<% 
    let page = htmlWebpackPlugin.options.pageData;
    let pageTpl = page.template.split('src/')[1]
%>

<!-- header -->
<%= require('@/common/header/header.ejs')({ page: page }) %>
<!-- main -->
<%= require('./' + pageTpl)({ page: page }) %>
<!-- footer -->
<%= require('@/common/footer/footer.ejs')({ page: page }) %>